apiVersion: apps/v1
kind: Deployment
metadata:
  name: spinnawinna-site-ppc-social
  labels:
    app: spinnawinna-site-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spinnawinna-site-ppc-social
  template:
    metadata:
      labels:
        app: spinnawinna-site-ppc-social
    spec:
      containers:
        - name: spinnawinna-site-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: spinnawinna-site-ppc-social
  labels:
    app: spinnawinna-site-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: spinnawinna-site-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER
